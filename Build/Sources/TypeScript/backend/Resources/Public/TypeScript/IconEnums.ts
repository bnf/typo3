export enum Sizes {
  small = 'small',
  default = 'default',
  large = 'large',
  overlay = 'overlay',
}

export enum States {
  default = 'default',
  disabled = 'disabled',
}

export enum MarkupIdentifiers {
  default = 'default',
  inline = 'inline',
}
